# Next Step

The previous product-direction note has been archived at
`docs/archive/nextstep-2026-04-16.md`.

It was last updated on 2026-04-16 and predates the current
`multi-window-test` branch work. Treat it as historical context, not the
active roadmap.

Current branch focus: polish and validate the multi-window editor, then decide
whether to merge it back to `main`.
